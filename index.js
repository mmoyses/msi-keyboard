var keyboard = require('msi-keyboard');

keyboard.color('left', { color: 'green', intensity: 'med' });
keyboard.color('middle', { color: 'blue', intensity: 'med' });
keyboard.color('right', { color: 'purple', intensity: 'med' });
